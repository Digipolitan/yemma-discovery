{
    "info": {
        "method": "POST",
        "json": true,
        "body": {
            "timeout": "?TIMEOUT",
            "realm": "$REALM",
            "port": "$PORT",
            "access_token": "$ACCESS_TOKEN"
        }
    }
}